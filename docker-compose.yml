services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: yiguan-backend
    restart: unless-stopped
    environment:
      - DB_PATH=/data/yiguan.db
      - JWT_SECRET=${JWT_SECRET:-yiguan-dev-secret}
      - LLM_API_KEY=${LLM_API_KEY}
      - SERVER_PORT=8080
    volumes:
      - ./data:/data
      - ./logs:/app/logs
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/api/admin/dashboard"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: yiguan-frontend
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-80}:80"
    depends_on:
      backend:
        condition: service_healthy
